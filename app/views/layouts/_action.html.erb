<% if collection.include?(current_user.id) %>
    <%= render "#{resource.type}s/un#{action}", resource: resource %>
<% else %>
    <%= render "#{resource.type}s/#{action}", resource: resource %>
<% end %>
