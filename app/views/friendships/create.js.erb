$("#fr-msg").text("<%= friendship_action(@friendship) %>");
$("#user-<%= @friendship.receiver_id %>-fr-btn").empty().append("<%= j render "layouts/action", action: "friend", resource: @friendship.receiver, collection: @friendship.receiver.friends.ids %>");
